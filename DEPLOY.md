# Invoice Generator

Admin dashboard and invoice generator built with React (Vite), Express, and Postgres (Supabase).

## Dev

```bash
cd backend && cp .env.example .env   # configure DB
npm run dev                          # Vite frontend
```